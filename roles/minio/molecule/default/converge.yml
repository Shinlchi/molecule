---
- name: Converge
  hosts: kess
  gather_facts: false
  tasks:
    - name: Configure MinIO
      ansible.builtin.include_role:
        name: poc.minio.minio
      vars:
        role: configure
        lib_arguments:
          server: "{{ minio_server }}"
          entries: "{{ test_minio_entries }}"
